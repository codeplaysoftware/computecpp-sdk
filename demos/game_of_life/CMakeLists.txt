project(GameOfLifeApp)

set(SOURCES
  "${CMAKE_CURRENT_SOURCE_DIR}/main.cpp"
  "${CMAKE_CURRENT_SOURCE_DIR}/sim.cpp")

ci_make_app(
  APP_NAME "GameOfLife"
  SOURCES "${SOURCES}"
  LIBRARIES "${OpenCL_LIBRARIES}"
  LIBRARIES "${COMPUTECPP_RUNTIME_LIBRARY}"
  CINDER_PATH "${CINDER_PATH}")

__build_spir("GameOfLife" "${CMAKE_CURRENT_SOURCE_DIR}/sim.cpp" "${CMAKE_CURRENT_BINARY_DIR}" 1)

